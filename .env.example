# Copy to .env
DATABASE_URL=postgres://ide:ide@localhost:5433/ide
PORT=4001
# Docker connection is auto-detected (named pipe on Windows, /var/run/docker.sock elsewhere)
SANDBOX_TIMEOUT_MS=10000
SANDBOX_MEMORY_MB=256
SANDBOX_PIDS_LIMIT=64
SANDBOX_CPU=0.5
